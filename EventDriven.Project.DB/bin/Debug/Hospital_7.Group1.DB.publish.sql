/*
Deployment script for Bank

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Bank"
:setvar DefaultFilePrefix "Bank"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Account].[CustomerID] is being dropped, data loss could occur.

The column [dbo].[Account].[ClientId] on table [dbo].[Account] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Account])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping Default Constraint unnamed constraint on [dbo].[Account]...';


GO
ALTER TABLE [dbo].[Account] DROP CONSTRAINT [DF__Account__Balance__3B75D760];


GO
PRINT N'Dropping Foreign Key unnamed constraint on [dbo].[Account]...';


GO
ALTER TABLE [dbo].[Account] DROP CONSTRAINT [FK__Account__Id__3D5E1FD2];


GO
PRINT N'Starting rebuilding table [dbo].[Account]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Account] (
    [Id]           INT             IDENTITY (1, 1) NOT NULL,
    [ClientId]     INT             NOT NULL,
    [Balance]      DECIMAL (15, 2) DEFAULT 0.00 NULL,
    [ModifiedDate] DATETIME        NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Account])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Account] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Account] ([Id], [Balance], [ModifiedDate])
        SELECT   [Id],
                 [Balance],
                 [ModifiedDate]
        FROM     [dbo].[Account]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Account] OFF;
    END

DROP TABLE [dbo].[Account];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Account]', N'Account';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Foreign Key unnamed constraint on [dbo].[Account]...';


GO
ALTER TABLE [dbo].[Account] WITH NOCHECK
    ADD FOREIGN KEY ([Id]) REFERENCES [dbo].[Client] ([Id]);


GO
PRINT N'Creating Procedure [dbo].[GetClientAccountDetails]...';


GO
CREATE PROCEDURE GetClientAccountDetails
AS
BEGIN
    SELECT 
        c.Id AS ClientId,
        c.Firstname,
        c.Lastname,
        c.MobileNumber,
        c.Gender,
        c.Address,
        c.Birtdate,
        a.Id AS AccountId,
        a.Balance,
        a.ModifiedDate
    FROM 
        [dbo].[Client] c
    INNER JOIN 
        [dbo].[Account] a ON c.Id = a.ClientId
    ORDER BY 
        c.Id;
END;
GO
PRINT N'Creating Procedure [dbo].[GetClientAccountDetailsById]...';


GO
CREATE PROCEDURE GetClientAccountDetailsById
    @ClientId INT
AS
BEGIN
    SELECT 
        c.Id AS ClientId,
        c.Firstname,
        c.Lastname,
        c.MobileNumber,
        c.Gender,
        c.Address,
        c.Birtdate,
        a.Id AS AccountId,
        a.Balance,
        a.ModifiedDate
    FROM 
        [dbo].[Client] c
    INNER JOIN 
        [dbo].[Account] a ON c.Id = a.ClientId
    WHERE 
        (c.Id = @ClientId) 
END;
GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/
INSERT INTO [dbo].[User]
(
[Username], 
[Password])
VALUES('admin','amin')


-- Insert records into Client table
INSERT INTO [dbo].[Client] (Firstname, Lastname, MobileNumber, Gender, Address, Birtdate)
VALUES 
('John', 'Doe', '123-456-7890', 'Male', '123 Main St, Springfield', '1985-03-25'),
('Jane', 'Smith', '234-567-8901', 'Female', '456 Elm St, Shelbyville', '1990-07-12'),
('Michael', 'Johnson', '345-678-9012', 'Male', '789 Oak St, Capital City', '1978-11-05'),
('Emily', 'Davis', '456-789-0123', 'Female', '101 Pine St, Ogdenville', '2000-01-15'),
('David', 'Wilson', '567-890-1234', 'Male', '202 Birch St, North Haverbrook', '1995-06-20'),
('Sarah', 'Brown', '678-901-2345', 'Female', '303 Maple St, Springfield', '1982-02-10'),
('Robert', 'Taylor', '789-012-3456', 'Male', '404 Cedar St, Shelbyville', '1988-09-15'),
('Linda', 'Martinez', '890-123-4567', 'Female', '505 Walnut St, Capital City', '1992-04-30'),
('James', 'Anderson', '901-234-5678', 'Male', '606 Ash St, Ogdenville', '1980-12-01'),
('Patricia', 'Thomas', '012-345-6789', 'Female', '707 Fir St, North Haverbrook', '1975-07-18');

-- Insert records into Account table
INSERT INTO [dbo].[Account] (ClientId, Balance, ModifiedDate)
VALUES 
(1, 1000.00, GETDATE()),
(2, 2500.50, GETDATE()),
(3, 500.75, GETDATE()),
(4, 3000.00, GETDATE()),
(5, 750.25, GETDATE()),
(6, 1200.00, GETDATE()),
(7, 2000.00, GETDATE()),
(8, 1800.75, GETDATE()),
(9, 150.00, GETDATE()),
(10, 5000.00, GETDATE());


GO

GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
CREATE TABLE [#__checkStatus] (
    id           INT            IDENTITY (1, 1) PRIMARY KEY CLUSTERED,
    [Schema]     NVARCHAR (256),
    [Table]      NVARCHAR (256),
    [Constraint] NVARCHAR (256)
);

SET NOCOUNT ON;

DECLARE tableconstraintnames CURSOR LOCAL FORWARD_ONLY
    FOR SELECT SCHEMA_NAME([schema_id]),
               OBJECT_NAME([parent_object_id]),
               [name],
               0
        FROM   [sys].[objects]
        WHERE  [parent_object_id] IN (OBJECT_ID(N'dbo.Account'))
               AND [type] IN (N'F', N'C')
                   AND [object_id] IN (SELECT [object_id]
                                       FROM   [sys].[check_constraints]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0
                                       UNION
                                       SELECT [object_id]
                                       FROM   [sys].[foreign_keys]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0);

DECLARE @schemaname AS NVARCHAR (256);

DECLARE @tablename AS NVARCHAR (256);

DECLARE @checkname AS NVARCHAR (256);

DECLARE @is_not_trusted AS INT;

DECLARE @statement AS NVARCHAR (1024);

BEGIN TRY
    OPEN tableconstraintnames;
    FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
    WHILE @@fetch_status = 0
        BEGIN
            PRINT N'Checking constraint: ' + @checkname + N' [' + @schemaname + N'].[' + @tablename + N']';
            SET @statement = N'ALTER TABLE [' + @schemaname + N'].[' + @tablename + N'] WITH ' + CASE @is_not_trusted WHEN 0 THEN N'CHECK' ELSE N'NOCHECK' END + N' CHECK CONSTRAINT [' + @checkname + N']';
            BEGIN TRY
                EXECUTE [sp_executesql] @statement;
            END TRY
            BEGIN CATCH
                INSERT  [#__checkStatus] ([Schema], [Table], [Constraint])
                VALUES                  (@schemaname, @tablename, @checkname);
            END CATCH
            FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
        END
END TRY
BEGIN CATCH
    PRINT ERROR_MESSAGE();
END CATCH

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') >= 0
    CLOSE tableconstraintnames;

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') = -1
    DEALLOCATE tableconstraintnames;

SELECT N'Constraint verification failed:' + [Schema] + N'.' + [Table] + N',' + [Constraint]
FROM   [#__checkStatus];

IF @@ROWCOUNT > 0
    BEGIN
        DROP TABLE [#__checkStatus];
        RAISERROR (N'An error occurred while verifying constraints', 16, 127);
    END

SET NOCOUNT OFF;

DROP TABLE [#__checkStatus];


GO
PRINT N'Update complete.';


GO
