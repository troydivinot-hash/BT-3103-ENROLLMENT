/*
Deployment script for Bank

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "Bank"
:setvar DefaultFilePrefix "Bank"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Client].[Birtdate] is being dropped, data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Client])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering Table [dbo].[Client]...';


GO
ALTER TABLE [dbo].[Client] DROP COLUMN [Birtdate];


GO
ALTER TABLE [dbo].[Client]
    ADD [Birthdate] DATETIME NULL;


GO
PRINT N'Altering Procedure [dbo].[CreateClientAccount]...';


GO
ALTER PROCEDURE [dbo].[CreateClientAccount]
    @Firstname VARCHAR(30) = NULL,  
    @Lastname VARCHAR(30) = NULL,  
    @MobileNumber NVARCHAR(30) = NULL,  
    @Gender VARCHAR(30) = NULL,    
    @Address VARCHAR(50) = NULL,   
    @Birthdate DATETIME = NULL,    
    @Balance DECIMAL(15, 2) = NULL
AS
BEGIN
    SET NOCOUNT ON;

        INSERT INTO [dbo].[Client] (Firstname, Lastname, MobileNumber, Gender, Address, Birthdate)
        VALUES (@Firstname, @Lastname, @MobileNumber, @Gender, @Address, @Birthdate);

        DECLARE @NewClientId INT = SCOPE_IDENTITY();

        INSERT INTO [dbo].[Account] (ClientId, Balance, ModifiedDate)
        VALUES (@NewClientId, ISNULL(@Balance, 0.00), GETDATE());
END;
GO
PRINT N'Altering Procedure [dbo].[GetClientAccountDetails]...';


GO
ALTER PROCEDURE dbo.GetClientAccountDetails
AS
BEGIN
    SELECT 
        c.Id AS ClientId,
        c.Firstname,
        c.Lastname,
        c.MobileNumber,
        c.Gender,
        c.Address,
        c.Birthdate,
        a.Id AS AccountId,
        a.Balance,
        a.ModifiedDate
    FROM 
        [dbo].[Client] c
    INNER JOIN 
        [dbo].[Account] a ON c.Id = a.ClientId
    ORDER BY 
        c.Id;
END;
GO
PRINT N'Altering Procedure [dbo].[GetClientAccountDetailsById]...';


GO
ALTER PROCEDURE dbo.GetClientAccountDetailsById
    @ClientId INT
AS
BEGIN
    SELECT 
        c.Id AS ClientId,
        c.Firstname,
        c.Lastname,
        c.MobileNumber,
        c.Gender,
        c.Address,
        c.Birthdate,
        a.Id AS AccountId,
        a.Balance,
        a.ModifiedDate
    FROM 
        [dbo].[Client] c
    INNER JOIN 
        [dbo].[Account] a ON c.Id = a.ClientId
    WHERE 
        (c.Id = @ClientId) 
END;
GO
PRINT N'Altering Procedure [dbo].[UpdateClientAccount]...';


GO
ALTER PROCEDURE [dbo].[UpdateClientAccount]
    @ClientId INT = NULL,      
    @Firstname VARCHAR(30) = NULL,  
    @Lastname VARCHAR(30) = NULL,   
    @MobileNumber NVARCHAR(30) = NULL, 
    @Gender VARCHAR(30) = NULL,    
    @Address VARCHAR(50) = NULL,   
    @Birthdate DATETIME = NULL,    
    @Balance DECIMAL(15, 2) = NULL,
    @ModifiedDate DATETIME = NULL  
AS
BEGIN
    SET NOCOUNT ON;
        UPDATE [dbo].[Client]
        SET Firstname = @Firstname,
            Lastname = @Lastname,
            MobileNumber = @MobileNumber,
            Gender = @Gender,
            Address = @Address,
            Birthdate = @Birthdate
        WHERE Id = @ClientId;

        UPDATE [dbo].[Account]
        SET Balance = ISNULL(@Balance, 0.00),
            ModifiedDate = @ModifiedDate
        WHERE ClientId = @ClientId;
    
END;
GO
PRINT N'Refreshing Procedure [dbo].[DeleteClientAccount]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[DeleteClientAccount]';


GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/
INSERT INTO [dbo].[User]
(
[Username], 
[Password])
VALUES('admin','amin')


-- Insert records into Client table
INSERT INTO [dbo].[Client] (Firstname, Lastname, MobileNumber, Gender, Address, Birtdate)
VALUES 
('John', 'Doe', '123-456-7890', 'Male', '123 Main St, Springfield', '1985-03-25'),
('Jane', 'Smith', '234-567-8901', 'Female', '456 Elm St, Shelbyville', '1990-07-12'),
('Michael', 'Johnson', '345-678-9012', 'Male', '789 Oak St, Capital City', '1978-11-05'),
('Emily', 'Davis', '456-789-0123', 'Female', '101 Pine St, Ogdenville', '2000-01-15'),
('David', 'Wilson', '567-890-1234', 'Male', '202 Birch St, North Haverbrook', '1995-06-20'),
('Sarah', 'Brown', '678-901-2345', 'Female', '303 Maple St, Springfield', '1982-02-10'),
('Robert', 'Taylor', '789-012-3456', 'Male', '404 Cedar St, Shelbyville', '1988-09-15'),
('Linda', 'Martinez', '890-123-4567', 'Female', '505 Walnut St, Capital City', '1992-04-30'),
('James', 'Anderson', '901-234-5678', 'Male', '606 Ash St, Ogdenville', '1980-12-01'),
('Patricia', 'Thomas', '012-345-6789', 'Female', '707 Fir St, North Haverbrook', '1975-07-18');

-- Insert records into Account table
INSERT INTO [dbo].[Account] (ClientId, Balance, ModifiedDate)
VALUES 
(1, 1000.00, GETDATE()),
(2, 2500.50, GETDATE()),
(3, 500.75, GETDATE()),
(4, 3000.00, GETDATE()),
(5, 750.25, GETDATE()),
(6, 1200.00, GETDATE()),
(7, 2000.00, GETDATE()),
(8, 1800.75, GETDATE()),
(9, 150.00, GETDATE()),
(10, 5000.00, GETDATE());


GO

GO
PRINT N'Update complete.';


GO
